# На том же VPS, что Urbanscore: порты 80/443 заняты его Caddy.
# Dofamy — только nginx на WEB_PORT (8082). HTTPS и сертификат — в Caddyfile Urbanscore.

services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8082}:80"
