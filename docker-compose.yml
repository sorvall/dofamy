# На том же VPS, что Urbanscore: порты 80/443 заняты его Caddy.
# Dofamy слушает WEB_PORT (по умолчанию 8082). HTTPS — через Caddy Urbanscore (см. Caddyfile.urbanscore-snippet).

services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8082}:80"
