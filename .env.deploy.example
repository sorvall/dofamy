# Скопируйте в .env.deploy и заполните (файл .env.deploy не коммитится).
# Те же значения, что в репозитории urbanscore, если сервер один.

SERVER_HOST=
SERVER_USER=root
SSH_PASSWORD=

# опционально
# DEPLOY_PATH=/root/dofamy
# SSH_PORT=22
